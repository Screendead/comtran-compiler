# 2026-08-16 · chunk B3, the three underdetermined rules

Chunk B3 is the arithmetic generator of M4 stage 2. It fills the mnemonic,
operand and OCTAL columns of every SET, every ADD, every truth function and
every subscript recomputation the 90.05 payroll sample compiles, at commit
1cdcb37 on `m4s2-chunk-b3`. Six rules carry it, and three of them are
underdetermined: one program attests each, and a rival formulation fits the
same ink.

All three items are DECIDED under the CLAUDE.md section 12 standing rule.
Silence lets them stand. The record asks nothing and authorizes nothing;
the B3 pull request waits for Jack.

| Path | What it holds |
|---|---|
| `index.html` | The record. Standalone; open it anywhere. |
| `evidence/result-storage-parks.txt` | Every result-storage reference in the object program, by section, with the block reservation and the section bases. |
| `evidence/add-corresponding-targets.txt` | `ADD CORRESPONDING WORKING TO MASTER TOTALS, INTERNAL.TOTALS` as the listing prints it, LOC 00733 to 00770. |
| `evidence/plain-add-targets.txt` | The one plain multi-target ADD, LOC 01265 to 01272, which keeps the written order. |
| `evidence/receiver-descriptions.txt` | WORKING, MASTER TOTALS and INTERNAL.TOTALS as the data division describes them. |
| `tools/build_doc.py` | Builds `index.html` from this directory. |
