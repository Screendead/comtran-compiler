# Brief for the review record `reviews/2026-09-13-m6s1-acceptance/`

Title: M6 stage 1, the sample's acceptance: the reconstruction, the page reading, and the four findings.

Date of the evidence: 2026-09-13. Branch `m6s1-sample`. Every repository link points at the commit hash given in RECORD_HEAD.

The record is built after the code landed, in one autonomous run, under the CLAUDE.md section 12 standing rule (a decision with one viable option does not stop and wait). Say so in the provenance block. No item blocks the pull request; one item asks Jack for an authorization he alone can give (item 2), and the pull request does not depend on it.

Sources to read before writing: `docs/design/m6-acceptance.md` (M6-1 to M6-5), `test/fixtures/90.05-tapes-notes.md`, `docs/design/m4-codegen.md` M4-20 (a) and its 2026-09-13 amendment, `docs/design/runtime.md` RT-3 and RT-5 and their 2026-09-13 amendments, `test/fixtures/90.05-report-page-217.txt`, `test/goldens/90.05-payroll.report`, `test/acceptance_test.dart`.

## Items, in order

### 1. SETTLED — The run reproduces the page up to four findings
The outcome. Show our report and the page reading side by side for the PAYFILE block (a monospace listing, not a crop), with the one differing cell marked. State: ERRORFILE identical (17 lines); CHECKFILE's three printed checks are our last three; 18 of 19 PAYFILE lines identical; bond order identical but one letter. Then the four findings as a table with their class (ours, fixed / the 1962 processor's, corroborated / print artifact / not settled, sealed until M7).

### 2. YOUR CALL — Authorize the erratum: the transcription's stagger note on PDF p. 217
Evidence: crop of the PAYFILE block raw (skewed) and the same block deskewed (rotate −1.83°) showing each line on one baseline. Quote the transcription note verbatim (from `comtran-manuals/J28-6169/90.05-sample-program.md`, the paragraph beginning "The illustrative run of the sample program"). Show the arithmetic: BLUT H's net 112.20 = 136.00 − 0.00 − 19.80 − 2.00 − 2.00 under the baseline reading; under the transcription's row assignment the first line has no net pay at all and an orphan line stands above it. The request: authorize (a) removing the sentence about the printer's practice, (b) reflowing the code block so each line's right-hand fields sit on its own row. Options: authorize both; authorize (a) only and leave the block; refuse and leave a HANDOVER note. Consequence of each. Recommend both. Note: the conversion is read-only until he says so, and nothing in this PR touches it.

### 3. SETTLED — RET.PREM equals INS.PREM: the 1962 processor's own defect, corroborated by its output
Evidence: crop of the page's INS.PREM and RET.PREM columns (pay-right region) showing equal pairs on all twelve lines; a crop of PDF p. 214 or 215 at LOC 01421–01432 (the two pointer builds from GN)091 and GN)093) and of PDF p. 216 at CP)+38 and CP)+39 (`PZE RETPREM-2` and `PZE INSPREM-2`, both `0 00000 0 00134`) — find the pages by grepping `test/goldens/90.05-payroll.storage-map` or the object-listing notes for the page that holds LOC 01421 and LOC 01742. The table in the source (`test/fixtures/90.05-payroll.ct`, TABLE) gives distinct premiums in every row: quote three rows. Explain the mechanism in plain words: both pointer words hold the same table word with byte 0, both step lists convert the first three characters, so the retirement premium read is the insurance premium. State what changed: M4-20 (a)'s consequence clause ("the byte selection lives in the generated lookup code") is withdrawn by the amendment; the pool words stay as printed and the golden is untouched. State what this means for M7: a real defect of the January 1962 processor, to be looked for in the 1963 source when the seal ends.

### 4. SETTLED — The grand-total net pay: the page says 2180.63, its own columns say 2183.83
Evidence: crop of the GT line's right end (gt-net region, PDF p. 217 around x 1240–1440, y 540–615) at 6×, showing 2180.63 unambiguous. The sums: the seven department nets (112.20, 80.60, 174.28, 236.52, 74.08, 165.76, 1340.39) = 2183.83; GT's own GROSS − WHT − FICA − BOND − INS − RET = 2730.39 − 449.35 − 5.21 − 36.00 − 28.00 − 28.00 = 2183.83. Ours prints 2183.83. State plainly: the same object text ran in 1961; the difference is in the 1961 runtime library or in something the page does not show; the manuals cannot say which; evidence exists and is sealed until M7 (D0.9). The acceptance test pins the difference. No decision is asked.

### 5. DECIDED — The reconstruction's chosen values
Derived fields need no decision; list them briefly (rate, exemptions, FICA year-to-date at the cap, bond deduction, WOO's denomination). The decided ones, each with the rejected alternative and its cost:
- The two ERRORFILE names `MOCRE J` and `MOCRE D` keyed as the ink reads. Rejected: `MOORE` (the likely intent, given the `WCO`/`WOO` artifact on the same page). Cost of the choice made: a later reader may think the data said MOCRE; cost of the alternative: the fixture would carry a reading the page does not show, and the acceptance diff would grow two artifact lines. Crop: errorfile region showing MOCRE beside JONES/BLOODSOE (closed O's).
- `WOO J` keyed from two of its three prints; the bond order's `WCO` recorded as a print artifact. Crop: check-bond region showing `2WOO J` and `WCO J`, and the PAYFILE `WOO J`.
- CASPERIAN's FICA year-to-date 0.00 (any value to 140.80 prints the page).
- WOO's bond accumulation 20.50, the least value that orders (any value 20.50–37.49 prints the page; after the order the master holds 0.00).
- SOBEK, REYNOLDS, WILLIAMS: denomination 18.75 and accumulation 0.00 (any denomination above accumulation + deduction prints the page; 18.75 is the price of a $25 US savings bond in the period — label as external period fact).
- Every field the report never prints: zero or blank. The DATE blank (overwritten by the detail date), TRIGGERS blank, the four year-to-date totals zero. Unmatched masters: number and name only.
- Detail blocks written as 14 words (3 + 11 blanks) per J 90.05.03 rather than 3-word blocks (both read the same; 14 is what the manual describes).
Overturn cost: regenerate two images and the report golden; nothing else moves.

### 6. DECIDED — The MOVPAK runtime preserves index register 1 and consumes the trailing AXT
Evidence: the object text at LOC 01320–01333 (quote from `test/goldens/90.05-payroll.code`: `LAC BL)2,1` at 01320, the SYS)180 sequence 01325–01331 ending `AXT 5,1`, `LDQ 1)EMPLOYEE.NUMBER,1` at 01333) and J 90.02.30's three-line calling-sequence print (crop from the J 90.02 page that prints SYS)267's sequence with the AXT line; find it by grepping `comtran-manuals/J28-6169/90.02-generated-code.md` for "NUMBER-OF-DIGITS-TO-CONVERT" and use the PDF marker above it). The page's bond order prints 091980, our first run printed 000000. The decision: save the incoming register at entry and restore it at the end of a call; the edited-store member owns two trailing words so the resume lands past the AXT. Rejected: a codegen reload of the register before 01333 (moves the object golden, which is byte-identical to the 1962 print — wrong by construction); restoring the register but still executing the AXT on the CPU (the AXT then sets the register to 5 and 01333 still reads address zero — tried, fails); consuming the AXT but not restoring (register stays 0 — tried, fails). Cite the amended RT-3 and RT-5. Pin: the protocol test asserts the sentinel comes back and the resume lands after the AXT. Consequence: every other output tape of the sample is byte-identical before and after (C1, D2, D3, D4); only the bond order changes.

### 7. SETTLED — The columns were measured on the scan
The scan agent's overlays (paths in `crops/` after copying from `/private/tmp/claude-501/-Users-jacklusher-development-comtran-compiler/bc4b8978-0f16-4e03-beea-182e219a80ed/scratchpad/agent-scan/`): `pay-l1-wholeline.png` (one PAYFILE line deskewed, full width), `check-294.png` (the `$` in column 41), `bond-left.png` (the bond-order strip's own origin), `error-M-line.png`. The numbers: rotation −1.83°; PAYFILE pitch 9.3314 px/col, origin 340.90 px; CHECKFILE 9.360 / 338.84; ERRORFILE 9.3174 / 1089.80. Table of measured vs expected (all MATCH for PAYFILE, CHECKFILE, ERRORFILE; BONDORDER a uniform −0.61 column offset from the CHECKFILE grid, internal pitch identical, glyph spacing as the layout). Say what would mislead: at page scale the right-hand fields look like they belong to the row above (show the raw crop), and the transcription was written from that view.

## Materials to ship
- `crops/`: every image above, cut from `comtran-manuals/J28-6169/images/page-217.png` and the object-listing pages with PIL; keep the white ground.
- `evidence/`: this brief as `brief.md`; the scan agent's `results.txt`; a copy of `test/fixtures/90.05-report-page-217.txt` and of the report golden.
- `tools/build_doc.py`: copied from the previous record at `/private/tmp/claude-501/-Users-jacklusher-development-comtran-compiler/bc4b8978-0f16-4e03-beea-182e219a80ed/scratchpad/prev-record/tools/build_doc.py` and rewritten for this content; RECORD_HEAD from the environment, default the commit hash given.
- `README.md`: the shape of the previous record's README (at `.../prev-record/README.md`).
